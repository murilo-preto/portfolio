# ========================
# DATABASE
# ========================
DB_HOST=mysql
DB_PORT=3306
DB_NAME=time_tracker

MYSQL_USER=app_user
MYSQL_ROOT_PASSWORD=SUPER_STRONG_ROOT_PASSWORD
MYSQL_DATABASE=time_tracker
MYSQL_PASSWORD=VERY_STRONG_PASSWORD

# ========================
# JWT
# ========================
JWT_SECRET_KEY=SUPER_LONG_RANDOM_STRING_64_CHARS_MIN
TOKEN_DURATION_HOURS=48

# ========================
# FLASK
# ========================
PORT=3000
FLASK_DEBUG=false

# Set to false to turn off API rate limiting (useful when running seed.py)
RATELIMIT_ENABLED=true

# Set to false to stop the daily sweep that completes finance entries whose
# planned date has passed (the tests turn it off)
SCHEDULER_ENABLED=true

# Shared between the Next.js proxy and Flask. It is what lets Flask believe an
# X-Forwarded-For header and rate-limit the real caller instead of the proxy
# container; without it a direct caller on port 3000 could choose their own
# rate-limit bucket. Leave empty to disable forwarding entirely — Flask then
# keys anonymous requests on the connecting address, which behind the proxy is
# one shared bucket. Any long random string.
INTERNAL_PROXY_SECRET=ANOTHER_LONG_RANDOM_STRING

